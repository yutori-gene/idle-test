
# 类别
对动作和物品进行分类的一般框架。
- 代表一个大分类。
- 例如，它可以在短程、远程、魔法、地下城、铁匠、制造厂等任何框架下进行配置。
- 动作和物品直接置于世界之下，并与类别 ID 关联。
___

## [_信息_](zh/编辑/信息)
___

## 群
显示类别的组分类
- 应用在 Basic 中配置的其中一个组。
- 类别列表会按照配置组的顺序显示。
- [_general_](zh/编辑/一般)
___

## 初始解锁状态
元素的初始显示与释放状态（满足触发条件后会自动进入更高阶状态）
- secreted：在满足所有要求之前，UI上将完全不会显示（提示中也不会出现，在要求/奖励列表中也会被隐藏）。
- hidden：在未被任何解锁元素引用且未满足条件的情况下，该内容将被完全隐藏（名称呈灰色且无法操作）。
- hinted（默认）：仅显示名称作为预告，但在解锁前无法进行操作。
- 已释放：无论条件如何，从一开始即可释放并可能进行操作（原“从一开始显示”）。
___

## [_条款和条件_](zh/编辑/要求)
___

## 装备组
指定可能装备该类别的物品组。
- 该类别中可装备物品的组名列表。
- 只有与此处指定的组名称相匹配的组中的物品才能装备。
- 如果指定的是空字符串，那么未配置组（空）的物品就有资格装备。
- 同一组中只能装备一件物品。装备新物品时，同组中的任何现有装备都会自动卸下。
- 如果列表为空（0 件），则无法装备任何物品。
- [_general_](zh/编辑/一般)
___

## 等级的可用性
类别等级功能的可用性
- 这是一个标志，表示该类别是否有等级。
- 如果开，则有等级；如果关，则没有。
- 一个例子是开根据经验划分的类别，如剑术和魔法，关根据地点划分的类别，如地下城和铁匠。
___

## 同时进行的进度
与其他类别并行执行的配置
- 这是一个标志，表示该类别中的动作是否可以与其他类别中的动作同时进行。
- 如果开，则动作可以同时进行，且不受其他类别动作的影响。
- 如果是关，则不能与其他类别中也配置为关的动作同时执行。在此类别中开始新动作时，其他非独立类别中正在进行的动作会自动中止。
___

## 背景
类别特定的背景配置
- 配置该类别的背景。
- 选择类别时，背景图片会随之改变。
- 如无配置，则按原样使用世界背景设置。