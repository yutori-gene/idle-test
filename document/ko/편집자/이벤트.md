
# 이벤트
조건에 따라 발동하는 메시지와 보상 시스템
- 설정한 타이밍과 조건에 일치할 때 발동하여 메시지를 표시합니다.
- 최초 실행 시, 게임 오버 시, 오프라인에서 복귀 시, 특정 조건 충족 시 등에 발동시킬 수 있다.
- 발동 시 카테고리 레벨, 액션 실행 횟수, 아이템 소지 수 등을 변경할 수 있습니다.
___

## [_인포메이션_](ko/편집자/정보)
___

## 타이밍
이벤트 발동 타이밍 설정

|타이밍|발동 조건|반복|
|-|-|-|
|`matched`|CONDITIONS에서 설정한 조건을 처음 충족했을 때|한 번만|
|`comebacked`|오프라인에서 1초 이상 꺼짐 후 복귀하고 진행 중인 액션이 있는 경우|여러 번|
|`gameovered`|전투에서 플레이어의 스태미나가 바닥났을 때|여러 번|
|`completed`|모든 카테고리의 레벨이 최대 값(maxCategoryLevels)에 도달하면|한 번만|
|`welcomed`|이 월드를 처음 시작했을 때|한 번만|
|`obtained`|single 유형의 액션(보물상자 등)을 완료하거나 확인했을 때. requirements를 충족하면 이벤트 보상도 추가됩니다.|여러 번|
___

## 초기 표시 상태
이벤트 아이콘의 초기 표시 상태 (발동 여부는 타이밍에 따라 결정되며, 이 설정에서는 발동하지 않습니다)
- 이벤트의 발동 조건은 타이밍에 따라 결정되며, 이 항목은 목록 아이콘의 외관에만 영향을 미칩니다.
- secreted: 모든 요구 사항을 충족할 때까지 이벤트 목록에 전혀 표시되지 않습니다(이벤트 발생 자체는 타이밍에 따라 일어납니다).
- hidden・hinted: 아이콘에 가로줄이 표시되어 잠금 상태로 나타납니다.
- released: 사선이 사라지고 '해제됨'으로 표시됩니다.
___

## 조건
이벤트 발동 및 보상 지급 조건
- 이벤트 발동 또는 보상 부여 조건입니다.
- `matched`에서는 이 조건을 충족할 때만 발동되어 보상을 획득합니다.
- `matched` 이외의 시점에서는, 타이밍 조건이 성립하면 발동합니다. 그 후, 여기서 설정한 조건을 충족할 때에만 보상을 획득합니다.
___

### 타입
조건으로 참조하는 요소의 종류

|타입|참조되는 값|
|-|-|
|카테고리|카테고리 레벨|
|액션|액션 실행 횟수|
|아이템|아이템 보유 수|
___

### 요소의 ID
조건으로 참조하는 요소의 ID
___

### 값
요구사항에 필요한 값의 수
- 지정한 값 이상인 경우 조건을 만족합니다.
___

### 소비 확률[0-1]
아이템이 소모될 확률(0~1, 타입이 아이템인 경우에만 유효)
- 조건을 충족하고 액션이 실행되었을 때 아이템이 소비될 확률입니다.
- 1`이면 반드시 소비, `0.5`이면 50% 확률로 소비, `0`이면 소비하지 않습니다.
- 기본값은 `1`(반드시 소비)입니다.
- 타입이 아이템이 아닌 경우 유효하지 않습니다.
___

### 장비
아이템이 장비 상태일 것을 조건으로 할 것인지(타입이 아이템인 경우에만 유효)
- 활성화하면 아이템을 소지하고 있을 뿐만 아니라 장비가 장착되어 있는 것이 조건으로 요구사항이 됩니다.
- 타입이 아이템이 아닌 경우 유효하지 않습니다.
___

## 보상
이벤트 발동 시 보상 설정
- 이벤트가 발동했을 때의 보상입니다.
- `matched`에서는 조건을 충족하여 발동되었을 때에만 보상을 받습니다.
- `matched` 이외의 경우, 타이밍 조건이 성립하고 conditions의 조건을 모두 충족할 때에만 보상을 획득합니다.
- 수량에 마이너스를 설정할 수 있습니다. 예를 들어 `게임오버`의 경우 아이템의 수량을 마이너스로 설정하면 게임 오버 시 아이템이 사라지게 됩니다.
___

### 타입
획득하는 요소의 종류

|타입|획득할 수 있는 것들|
|-|-|
|카테고리|레벨(경험치 환산으로 합산)|
|액션|실행 횟수|
|아이템|보유 수|
___

### 요소의 ID
획득할 요소를 식별하는 ID
___

### 값
취득하는 값
- 마이너스 값을 설정하면 소지 수, 실행 횟수, 레벨이 감소합니다. 단, 0 아래로는 내려가지 않습니다.
- 아이템에 최대 소유 수(MAXIMUM)가 설정되어 있는 경우, 그 값을 초과하여 소유 수가 증가하지 않습니다.
- 타입이 카테고리의 경우 설정한 값이 레벨에 직접 추가됩니다(1이면 1레벨, 0.5이면 0.5레벨). 액션의 경험치 설정에서 조정하는 것이 일반적인 방법입니다. 특별한 목적이 없는 한 이 설정은 불필요합니다.
___

### 확률 [-1~1]
획득 성공 확률 (음수는 실패 시에만 평가됨)
- 획득 확률을 -1에서 1 사이의 소수로 설정합니다. 확률 판정에는 절대값이 사용됩니다.
- 값이 양수일 경우, 액션 성공 시에만 평가되며, `1`일 때는 반드시 획득하고, `0.5`일 때는 50%의 확률로 획득합니다.
- 값이 음수일 때는 액션 실패 시에만 평가되며, `-1`일 경우 반드시 획득하고, `-0.5`일 경우 50%의 확률로 획득합니다.
- `0`인 경우 성공 시나 실패 시 모두 획득할 수 없습니다.
- 기본값은 `1`(성공 시 반드시 획득)입니다.
___

## 태스크 그룹
작업의 그룹 식별자(현재 사용되지 않음)
- 현재 이 항목은 사용되지 않습니다. 향후 기능 확장을 위해 예약되어 있습니다.